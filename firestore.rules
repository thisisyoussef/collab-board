rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /boards/{boardId} {
      // Public link mode: anyone with board URL can read/write without auth.
      // WARNING: this exposes board data to public internet access.
      allow read, write: if true;
    }
  }
}
